# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (SECURITY CRITICAL — never commit secrets)
.env
.env*.local
.env.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Third-party skills installed via `npx skills add` (tooling, not app code).
# Project-authored skills live directly in .claude/skills/<name>/ and ARE
# tracked — only the impeccable symlink (installed, not authored here) is
# excluded.
.agents/
.claude/skills/impeccable
skills-lock.json

# Agent worktrees (transient, auto-cleaned)
.claude/worktrees/

# Database exports — contain student personal data, never commit
/backups/
